<Window x:Class="ContractMonthlyClaimSystem2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="260" Width="380" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Contract Monthly Claim System" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <Label Content="Email"/>
            <TextBox Name="txtEmail" Width="320" Height="24"/>
            <Label Content="Password" Margin="0,8,0,0"/>
            <PasswordBox Name="txtPassword" Width="320" Height="24"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Name="btnLogin" Content="Login" Width="100" Click="Login_Click"/>
            <Button Name="btnRegister" Content="Register (demo)" Width="120" Margin="12,0,0,0" Click="Register_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Tip: use Register to create a demo account." HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,12,0,0" Foreground="Gray"/>
    </Grid>
</Window>
